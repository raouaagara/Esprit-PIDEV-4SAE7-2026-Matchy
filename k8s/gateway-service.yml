apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: devops-microservices
spec:
  selector:
    app: api-gateway
  ports:
    - protocol: TCP
      port: 8222
      targetPort: 8222
      nodePort: 30222
  type: NodePort
